import React from "react";
import classes from "./noMatches.module.css";
const NoMatches = () => {
  return <h3 className={classes["no-data"]}>No Matches Available</h3>;
};

export default NoMatches;
